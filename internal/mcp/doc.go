// Package mcp is a placeholder for the MCP server (streamable HTTP at /mcp)
// and the stdio proxy. It is not written yet.
//
// No date is promised. MCP is an adapter around the lease contract rather than
// a goal of its own, so it ships only if an agent workflow is recorded as
// having failed on the REST, CLI and Playwright paths first.
package mcp
